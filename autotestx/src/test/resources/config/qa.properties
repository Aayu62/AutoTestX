# AutoTestX — QA Environment Configuration
# Used when running: mvn test -Denv=qa

# ── API Configuration ────────────────────────────────────────────────────────
base.url=http://localhost:8080
request.timeout.ms=10000

# ── Default Credentials ──────────────────────────────────────────────────────
user.email=user@bookverse.com
user.password=Admin@123
admin.email=admin@bookverse.com
admin.password=Admin@123

# ── Database (PostgreSQL) ────────────────────────────────────────────────────
db.url=jdbc:postgresql://localhost:5432/bookversedb
db.username=bookverse
db.password=bookverse123

# ── Test Settings ────────────────────────────────────────────────────────────
max.retry.count=3
response.time.threshold.ms=500
parallel.threads=4
