# AutoTestX — Docker Environment Configuration
# Used when running inside Docker: mvn test -Denv=docker

base.url=http://bookverse-api:8080
request.timeout.ms=30000

user.email=user@bookverse.com
user.password=Admin@123
admin.email=admin@bookverse.com
admin.password=Admin@123

db.url=jdbc:postgresql://postgres:5432/bookversedb
db.username=bookverse
db.password=bookverse123

max.retry.count=3
response.time.threshold.ms=2000
parallel.threads=4
